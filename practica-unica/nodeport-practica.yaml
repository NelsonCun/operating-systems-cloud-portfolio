apiVersion: v1
kind: Service
metadata:
  name: nodeport-practica
spec:
  type: NodePort
  selector:
    run: pod-practica
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080